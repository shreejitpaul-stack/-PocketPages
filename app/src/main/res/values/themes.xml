<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PocketPages" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Light Theme Colors -->
        <item name="colorPrimary">@color/light_primary</item>
        <item name="colorPrimaryVariant">@color/light_primary_variant</item>
        <item name="colorSecondary">@color/light_secondary</item>
        <item name="colorOnPrimary">@color/light_onPrimary</item>
        <item name="android:colorBackground">@color/light_background</item>
        <item name="colorSurface">@color/light_surface</item>
        <item name="colorOnBackground">@color/light_onBackground</item>
        <item name="colorOnSurface">@color/light_onSurface</item>
        <item name="colorOutline">@color/light_outline</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/light_background</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/light_surface</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!-- Custom Button Styles -->
    <style name="Widget.PocketPages.Button" parent="Widget.Material3.Button">
        <item name="android:minHeight">@dimen/button_height_md</item>
        <item name="cornerRadius">@dimen/corner_radius_md</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:letterSpacing">0</item>
        <item name="elevation">@dimen/elevation_sm</item>
    </style>

    <style name="Widget.PocketPages.Button.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:minHeight">@dimen/button_height_md</item>
        <item name="cornerRadius">@dimen/corner_radius_md</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:letterSpacing">0</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <style name="Widget.PocketPages.Button.Text" parent="Widget.Material3.Button.TextButton">
        <item name="android:minHeight">@dimen/button_height_md</item>
        <item name="cornerRadius">@dimen/corner_radius_md</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:letterSpacing">0</item>
    </style>

    <!-- Card Styles -->
    <style name="Widget.PocketPages.Card" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">@dimen/corner_radius_md</item>
        <item name="cardElevation">@dimen/elevation_sm</item>
        <item name="android:layout_margin">@dimen/spacing_sm</item>
        <item name="contentPadding">@dimen/spacing_md</item>
    </style>

    <!-- EditText Styles -->
    <style name="Widget.PocketPages.EditText" parent="Widget.Material3.TextInputEditText.OutlinedBox">
        <item name="android:background">@drawable/edittext_background</item>
        <item name="android:padding">@dimen/spacing_md</item>
    </style>

    <!-- No Action Bar variant -->
    <style name="Theme.PocketPages.NoActionBar" parent="Theme.PocketPages">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>